import {Store, Persistor} from './configureStore';
import Types from './types';
import Actions from './actions';
import Reducers from './reducers';

export default {
  Store,
  Persistor,
  Types,
  Actions,
  Reducers,
};

export {Store, Persistor, Types, Actions, Reducers};
